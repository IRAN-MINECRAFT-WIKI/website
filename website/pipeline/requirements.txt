"""
requirements.txt — MineBed Pipeline dependencies

⚠️  NOTE: The original file accidentally contained HuggingFace
and GitHub tokens as plain text lines. This version is clean.

To install:   pip install -r requirements.txt
"""

playwright>=1.40.0
requests>=2.31.0
Pillow>=10.0.0
openai>=1.0.0
huggingface_hub>=0.20.0

# Optional: for content quality checks
python-dotenv>=1.0.0
